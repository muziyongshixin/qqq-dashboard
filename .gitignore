# ══════════════════════════════════════════════════════════════════
# 白名单模式：默认忽略一切，只显式放行要发布到 GitHub 的内容
# ══════════════════════════════════════════════════════════════════
# 为什么用白名单而不是黑名单：
#   本目录同时是「本地研究工作区」和「GitHub Pages 部署仓库」。
#   研究侧有几十个回测脚本、中间数据、策略报告，以及 .codebuddy 记忆文件，
#   这些**不应该**推到公开仓库。
#   黑名单容易漏（新增一个脚本就可能被误提交），白名单不会。
#
# 想额外发布某个文件时，在下面加一行 !路径 即可。

# ── 默认忽略根目录下的一切 ──
/*

# ── 放行：看板本体（GitHub Pages 的站点内容）──
!/dashboard/

# ── 放行：CI 工作流 ──
!/.github/

# ── 放行：仓库说明与本文件 ──
!/README.md
!/.gitignore

# ══════════════════════════════════════════════════════════════════
# dashboard 内部的排除项
# ══════════════════════════════════════════════════════════════════
__pycache__/
*.pyc
*.pyo
.DS_Store
.venv/
venv/

# ⚠ 注意：dashboard/data/*.json **必须提交**（不要加进忽略）。
#   prices.json 是 tdxrs 权威基线快照，CI 的增量更新依赖它作为拼接基线；
#   dashboard.json 是前端直接消费的数据。
#   两者都提交后，即使某天 tdxrs 临时不可用，页面也仍能显示上次的正确数据。
